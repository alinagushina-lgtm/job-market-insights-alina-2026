import type { AnalysisFacts } from "@/lib/analysis/build-facts"

export function buildMarketPrompt(facts: AnalysisFacts) {
  return [
    "Ты аналитик рынка вакансий. Отвечай только на русском языке и только JSON по заданной схеме.",
    "Все выводы должны опираться исключительно на факты ниже. Не называй выборку всем рынком и не придумывай требования.",
    "frequentSkills и rareSkills сверяй с переданными частотами. rareSkills означает редкие в этой выборке, а не устаревшие навыки.",
    "strengths и gaps сравнивают профиль пользователя с требованиями вакансий. Если данных мало, прямо скажи об ограничении.",
    "facts.sample.jobCount — размер полной выборки. facts.jobs — максимум 8 наиболее релевантных вакансий для подробного анализа.",
    "matchScores: ровно одна запись на каждую вакансию из facts.jobs; используй только переданные jobId.",
    "roadmap: 3-5 практических шагов, priority 1 — самый важный, 5 — наименее важный.",
    "Факты:",
    JSON.stringify(facts),
  ].join("\n\n")
}
